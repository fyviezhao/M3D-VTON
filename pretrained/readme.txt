Please put the MTM, DRM, and TFM pretrained models
in the corresponding subdirs under the 'pretrained' dir.

The correcet dir sturcture should be:

+ M3D-VTON
    ...
    + pretrained
        + aligned
            + DRM
                - latest_net_DRM.pth (255.36MB)
            + MTM
                - latest_net_MTM.pth (152.62MB)
            + TFM
                - latest_net_TFM.pth (81.39MB)
    ...